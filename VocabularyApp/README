1.create program(filename), a program to run the app;

2.program would initiate a dictionary(read from the filename), an empty user list, and a rank system

3.then we should get input from the UI to input username and add new user using addUser(Username)

4.a new user would have two events, one is flash-card, another is bag-of-words
 we can initiate the activity by using the Dictionary.handleReuqest(event e)
 to call the inbuilt function in the event createDataStructure(list)
 
5.then we can start the events for the user, 
update feedback(time, known) of words, then flashcard.insert back to the flash-card heap

6.we should be able to switch between users using getUser() in program and get rank in the program Rank system, getRank()